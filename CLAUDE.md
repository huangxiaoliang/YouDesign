# CLAUDE.md

本项目的 AI 协作约定、架构、踩坑铁律、协作流程等**统一以 [`AGENTS.md`](./AGENTS.md) 为唯一真源**。

请先完整阅读 `AGENTS.md` 再开始工作。约定有变更也只改 `AGENTS.md`，不要在此另写一份，避免两份说法漂移。

快速自检（详见 AGENTS.md）：

```bash
npm run typecheck && npm run build
```
