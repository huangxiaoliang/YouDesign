# AGENTS.md · AI 协作约定（唯一真源）

> 本文件是本项目所有 AI 编码工具（Claude Code / CodeX / 其它）和人类协作者的**唯一约定真源**。
> `CLAUDE.md` 只是指向本文件。改约定请改这里，不要在别处另写一份。

## 0. 项目是什么

YouDesign：面向**产品经理**的原型设计 Agent。用自然语言（可附截图/HTML/HTML 资源包 ZIP/Word/MD）描述页面，几秒到分钟级生成**接近现有产品风格的高保真可交互原型**。

产出形态（当前公开版**仅原生 HTML**）：

- **原生 HTML**：自包含 HTML（内联 CSS/JS），`srcDoc` 渲染，可离线打开、可在预览里直接改。

> 历史的双轨组件库生成路径（PC + 移动端 runtime）、内网分享、特定产品档案已为公开版整体剥离；相关代码路径（`src/lib/dpl/`、`public/{pc,mobile}-runtime/`、`vendor/`、`extract-*`/`build-*-runtime` 脚本）已移除。

**原则上只生成单页原型**（默认 1 个主页面；详情/新建/编辑等次级视图不单独生成；仅极少数确实独立的页面才多页，上限 3）。会话**自动持久化到浏览器**（IndexedDB）：刷新恢复现场、可查看/切换历史会话。

上传 HTML/ZIP 时进入**原样 HTML 路径**（不再有「原样打开」勾选框，HTML 上传即走此路径）：ZIP 在前端转成自包含 HTML（内联相对 CSS/图片/字体等资源，脚本禁用）。后端先用一次 **意图识别**（`classifyHtmlUploadIntent`，见 §2 `intent` 阶段）判定 `open/edit/regenerate/ask`：无文字直接 `open` 原样渲染；`regenerate` 参考上传页重新生成（走正常生成管线、自然套用已选风格）；`edit` 在原页上最小修改（可选 `applySelectedStyle` 把已选风格套到本次编辑）；`ask` 原样打开并直接回答问题。

护城河在「需求→结构化→生成→真实渲染」这条管线和提示词，不在外壳。面向使用者的总览见 `README.md`。

## 1. 怎么跑 / 怎么自检

```bash
# 安装：公网 npm registry，直装即可
npm install
#   注：若走代理，带「装后下原生二进制」的包（如 esbuild）需显式传 npm 代理参数，否则二进制拉取失败：
#   HTTPS_PROXY=http://127.0.0.1:7890 HTTP_PROXY=http://127.0.0.1:7890 npm install -D esbuild --proxy http://127.0.0.1:7890 --https-proxy http://127.0.0.1:7890

# 跑（生产方式，推荐）：先 build 再 start
npm run build && npm run start        # http://localhost:3000/youdesign

# 可选：临时外网分享（需先安装 cloudflared；本地服务已启动后再跑）
npm run tunnel                        # 默认穿透 http://localhost:3000/youdesign
npm run tunnel -- 3001                # 指定端口

# 桌面客户端
npm run desktop:dev                   # 开发壳；默认开发态可走本机服务
npm run desktop:dev:online             # 跨平台线上 Web 开发壳；Windows 开发/验收使用
npm run desktop:pack:mac              # 无签名 .app；打包态默认加载线上 Web
npm run desktop:dmg:mac              # 无签名 DMG；正式分发前仍需签名/公证
npm run desktop:dmg:thin:mac          # 薄客户端 DMG：线上 Web 壳 + Claude bridge，不带 .next/standalone
npm run desktop:nsis:thin:win         # Windows 10/11 x64 薄客户端 NSIS

# 自检三连（改完必过）
npm run typecheck                     # tsc --noEmit
npm run build
# 功能自检：登录后发一句需求，确认流式阶段走完到 done、右侧出预览

# 纯本地回归（无需服务/模型 key，改了对应链路必跑）
npm run test:desktop-claude-core       # 桌面 Claude 单条路径 + claude-html-utils 守卫 + 队列/取消
npm run test:desktop-claude-fragments  # 多片段事务提取、独立校验、原子回填、Contract/导航守卫
npm run test:desktop-windows-runtime  # Windows CLI/Git Bash/命令启动/协议/NSIS 配置回归
npm run test:wpl001-leftmenu          # LeftMenu text 字段数据铁律（历史 DPL 知识库产物，仍生效）
npm run test:prototype-contract       # Prototype Contract + must 交互可演示验收
npm run test:preview-navigation       # 原型真实导航拦截（静态门禁 + 预览守卫）
npm run test:export-history-bridge     # 导出移动窄框 history bridge 注入外层文档
npm run test:preview-device-mode       # 预览视口设备模式 + 手机外壳
npm run test:direct-html-editor       # 原生 HTML 单元素属性编辑 + 属性单位/校验 + 父级层级 + 撤销/重做/重置 + 临时标记清理
npm run test:session-brief            # 会话级 SessionBriefV1
npm run test:html-merge               # 多 HTML 主次页解析 + 抽屉/新页面分流 + 多级新页面合并
npm run test:capture-sanitization     # 抓取扩展净化
npm run test:captured-page-safety     # 抓取页离线安全
npm run test:captured-page-groups     # 抓取页离线重建页签组
npm run test:capture-drawer           # 抓取页交互 runtime（抽屉/模态框开关、父子级联、Escape、页签切换、iframe 重建，Playwright 实跑）
npm run test:capture-overlay          # 多页签采集浮窗
npm run test:usage-jsonl             # JSONL 用量存储
npm run test:mysql-usage             # MySQL 连通+表结构+事务写读回滚
```

- **`dev`/`start` 脚本内置 `env -u ANTHROPIC_BASE_URL -u ANTHROPIC_AUTH_TOKEN -u ANTHROPIC_API_KEY`**：Claude Code 会向子进程注入 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`，与本工具自有的 Anthropic key 冲突（走错端点/鉴权失败）。直接 `next dev`/`next start` 会踩；务必走 `npm run dev`/`npm run start` 让脚本先 unset 这些变量。
- **不要**用 `npm run dev` 配 `rm -rf .next`：偶发整页 500。验证一律用 `build && start`。
- 登录：默认 `YOUDESIGN_AUTH_MODE=auto` 时由 `data/users.json`（`scripts/user.mjs add <name>` 开户）驱动；无用户表时回退 `.env.local` 的 `YOUDESIGN_ACCESS_PASSWORD` 单口令（登录为匿名 default）。`YOUDESIGN_AUTH_MODE=shared` 强制共享口令；会话签名密钥 `YOUDESIGN_AUTH_SECRET`。
- 无密钥时设 `YOUDESIGN_FORCE_MOCK=true` 可离线跑通闭环。
- 桌面版默认加载 Web `http://localhost:3000/youdesign`（可用 `YOUDESIGN_DESKTOP_WEB_URL` 覆盖），复用 Web 登录、模型配置和用量体系；设 `YOUDESIGN_DESKTOP_USE_LOCAL_SERVER=true` 才启动本机 Next standalone。无论页面来自 Web 还是 Electron 内的本机 Next server，大 HTML 编辑都由服务端发 `desktop-claude-required`，前端经 preload bridge 预检并调用 `runClaudeHtmlEdit`，最终只有 Electron 主进程可以拉起 Claude CLI。本机 server 只承载 Web/API，不保留服务端 Claude Agent、独立队列或取消 API。

## 2. 架构与生成管线

入口 `POST /api/generate`（NDJSON 流式，需登录 cookie）→ `src/lib/pipeline/orchestrator.ts`：

```
HTML/ZIP 上传 rawHtml：ZIP 前端转自包含 HTML；先过 intent 意图识别（下）再分流
intent 意图识别   编辑模式 classifyEditIntent 判「改原型 vs 提问」，提问直接对话回答、不动原型；生成模式+HTML 上传 classifyHtmlUploadIntent（flash）判 open/edit/regenerate/ask（空文字直接 open）；生成模式+图片上传 classifyImageUploadIntent（视觉模型）判 ask/generate/generate-with-changes（空文字直接 generate）。ask 只回答不生成；regenerate 走正常生成管线、edit 走原页最小修改（可选 applySelectedStyle 套已选风格）
⓪ 预检 preflight   一次 flash 判「需求是否明确」+「PC/移动端 device」，模糊则反问暂停。**快速图片模式**（fastMode && hasImages && !hasDocs）跳过本次 LLM 预检，改用 inferDeviceByRule 规则判端 + 单页直出，timing 记 rule-skip
① 结构化 structure  默认单页 FlowSpec（罕见才多页）并产出 prototypeContract（页型/用户首要任务/mustHave/交互 trigger→result→proof/关键状态/假设）；有截图时附轻量 visualReference（faithful/layout/style/content + preserve/change/infer，冲突优先级为用户修改>截图事实>已选风格>模型推断）；快速模式恒走 flash，高质量模式按复杂度选 flash/pro；按 device 取目录；快速图片模式以确定性规则补默认视觉合同、零新增调用
② 检索 retrieve     （当前原生 HTML 路径不依赖外部组件库检索，语义召回仅历史 DPL 路径使用、已剥离；embedding 适配器仍在 src/lib/embeddings/，可复用）
③ 生成 generate     快速模式走 flash；高质量模式按场景分级：带图/带文档/复杂/多页/风格档案/还原类走强模型（pro/GLM），简单原生页 flash-first 出首版（结构自检不过再升 pro 重试一次，见 §4；结构自检当前临时跳过、此重试暂不触发，见 §6）。流式产出自包含 HTML；styleHead 经 buildStyleHead 注入（含产品风格档案 token）
③.5 校验 validate   真实导航静态门禁（unsafePrototypeNavigation 作修复项，2 轮修复仍含导航则返回兜底页 + blockedNavigation，runEdit 识别后保留原页），必要时自修复；原生 HTML 在 generate/runEditRawHtml 完成后**原设计**由 repairUnsafeHtmlNavigation 单独做一次 LLM 定向修复（失败不阻塞、交预览期守卫兜底）；**当前临时停用**（见 §6）；上传原样打开不改文件、只提示
③.55 结构自检 structure-check  **当前临时无条件跳过**（SKIP_NON_CLAUDE_STRUCTURE_CHECK=true、structureCheckStage 函数头直接 return []，checkGeneratedStructure 不再调用；快速/高质量模式均不跑通用结构自检、prototypeContract must 交互的确定性验收也随之不执行、失败不再定向优化，恢复见 §6）。原设计：通用结构自检只在高质量模式运行；must 交互在快速/高质量都做确定性验收（事件处理 + 可见状态变化 + 抽屉/弹窗/页签等目标形态），失败进入一次定向优化
③.6 自评审 review   快速模式跳过通用评审，但 must 交互验收失败会定向优化一次；高质量模式按风险分级：结构自检已发现问题直接定向优化、低风险直接跳过、仅风险信号才跑 flash 评审。**当前临时整体跳过**（reviewAndRefine 函数头直接 return code，见 §6）
③.7 品牌色矫正 normalize 生成/review/edit 后兜底：normalizeBrandColors 按 themeCss 饱和 token 就近吸附漂移色（如 #FF8060→#FF8040），中性灰要求精确匹配
④ 预览 preview      原生 HTML → srcDoc；统一经 guardPreviewNavigation 注入临时导航守卫（拦非锚点链接/表单提交/window.open），iframe 跳离 about:srcdoc 自动恢复原预览；守卫还在 iframe 内维护**统一回退栈**供企微外壳返回/回首页使用（__ydGoBack/__ydGoHome/__ydCanGoBack，追踪 hash/pushState、class 与内联 display 显隐切换、整页 DOM 挂载/卸载，同批变化合并一条记录整批还原，入栈记录滚动位置、回退时还原）；超宽表格按需包横向滚动容器、分离式表头/表体同步滚动（data-yd-preview-table-scroll，预览期补丁）；序列化/导出前 stripPreviewNavigationGuard 剥除守卫与滚动容器
⑤ 迭代 edit          HTML 产物用 HtmlSizeInfo 统一体量口径：普通对话框编辑按原始 HTML 体积判断，≥640KB 统一发 desktop-claude-required 事件给 Electron bridge（事件携带 editHtml+assets+instruction+focus（buildClaudeEditFocus 提前做的编辑计划+DOM 锚点，桌面端按此把任务收窄到 scope），bridge 直接复用资源占位 HTML 无需重建；运行中经 desktop-claude-progress 事件推各阶段进度；新增两个 no-op 终态出口：Claude 流末 isTrivialNoOp(html, edited) 命中时，extractClaudeClarification/extractClaudeAlreadySatisfied 从 Claude summary 解析——识别 YD_NEEDS_CLARIFICATION:/YD_ALREADY_SATISFIED: 显式标记或兼容信号，分别返回 {ok:false,needsClarification,clarification}/{ok:false,alreadySatisfied,message}，前端生成对应 assistant 消息、不产新版本；buildClaudeEditFocus 新增 scope 拒绝：scope 内缺 targetText 或页签触发+面板跨区域时不下放 scope；点选修改（内部仍称标注/annotation 链路）保持 tryScopePatch 优先：编辑前先 planHtmlEdit 结构化路由（judges.ts，flash 出 EditPlan：operation/scope/batch/interactive/needsFullPage）--交互/批量/needsFullPage 直接跳过单点 scope patch 交上游整页/Claude，否则把 plan 目标喂 locateScopeTarget；buildDomSummary 每行带 [offset N] + role/aria/data-* 业务属性，matchLocateToAnchor 优先按 offsetHint 精确定位；删除走快路径 directDeleteElement；隐藏/不显示/不可见走 directHideElement（注入 display:none !important、不调 LLM）；color/attr/text 走 tryDeterministicScopePatch（scope 内恰好命中 1 处的确定性替换、不调 LLM）；三者均在 LLM scope patch 之前、命中即 return、全分支埋点 outcome。对话式（无点选标注）编辑进 scope patch/Claude 前另跑一次整页 tryDeterministicScopePatch（裸「把 X 改成 Y」整页恰好命中 1 处/单一颜色/单一属性、0 LLM、与页面大小无关）。点选去重（EditPlan.operation=dedup）走 selectDedupScope 沿锚点祖先链找含≥2 同签名直接子的最小容器一次性去重；静态 HTML 无重复项则 return null 升级整页。未命中后资源占位 <640KB 回退整页编辑、≥640KB 再走 Claude/降级提示；所有产出点过 validateEditedHtmlDoc 结构完整性校验；isTrivialNoOp 空改检测与 hasInteractionDelta 交互增量检测**当前临时屏蔽**（见 §6）；页签类指令另检可执行点击切换逻辑（hasTabSwitchImplementation 未停用）；编辑产出原设计统一过真实导航门禁；送模型/Claude 前 compactDataUris 把 data URI 占位、输出后 expandDataUris 回填
```

**runEdit 的 Phase-1 护栏**（见 scripts/test-iter-edit-dpl.mjs 历史用例，逻辑通用）：① 模型返回非可解析 HTML（looksLikeHtml 不通过）-> 不覆盖原页、回澄清、不发 code/done；② isTrivialNoOp（归一相等）-> 保留原页、不产新版本；③ looksRewritten（长度比 <0.55，deleteMode 豁免）判整页重写 -> 升级 generate 阶段强模型带「逐字保留基底、只插入本次要求」纠正指令重试一次。

**device（pc/mobile）是关键分流键**：⓪ 预检从需求文字自动判定（手机/H5/App/小程序→mobile；后台/管理系统→pc；判不准则反问；兜底 pc），贯穿 getCandidates/generateSystemPrompt/validateStage。

登录门禁见 src/middleware.ts（未登录：页面跳 /login、接口返 401）。

**性能特征（重要）**：默认「快速模式」（fastMode 默认开）几秒到几十秒先出可改原型；高质量模式 generate 按场景分级（带图/带文档/复杂/多页/风格档案/还原类走 deepseek-v4-pro/GLM，简单原生页 flash-first）。deepseek-v4-pro 是推理模型、出首字节前可能思考约 90s。各阶段耗时已埋点（data/stage-timing.jsonl，见 §3）。注：③.55 结构自检 / ③.6 自评审/定向优化当前均临时整体跳过（见上），恢复前高质量模式实际也不跑 structure-check / review/refine。提速杠杆（按收益）：①保持默认单页、减少输出 ②多页按页并行生成 ③精简注入的 styleSpec ④按 stage-timing 实测调路由。

## 3. 改动地图（改什么去哪）

| 要改 | 去哪 |
|---|---|
| 模型路由 / 开关 | `.env.local` 的 `ROUTE_*` / `SELF_REVIEW`；基础配置在 `src/lib/config.ts`，用户模型选择分流在 `src/lib/providers/index.ts`，快速模式从 `src/app/page.tsx` → `src/app/api/generate/route.ts` → `orchestrator.ts` 传递 |
| 提示词（**影响产品质量，谨慎**） | `src/lib/prompts.ts`（含 Prototype Contract 结构化与生成/评审验收、截图 Visual Reference Contract 的 mode/preserve/change/infer 边界与固定冲突优先级、must 交互必须具备 trigger→result→proof 且禁 alert/toast 冒充、单页策略、device 分流、isTrivialNoOp 假成功检测、looksRewritten 整页重写检测、imagePlaceholderRule 占位图禁用、**严禁真实页面导航规则**、editPlanSystemPrompt（operation 含 dedup、scopeHint 取值集合含 tab/table-column、复合筛选+表格列规则驱动 LLM 输出 batch=true/needsFullPage=true/scopeHint=page、整列删除→scopeHint=table-column、移动→operation=move）、locateScopeSystemPrompt、interactiveEditIntentSystemPrompt、globalVisualEditIntentSystemPrompt（全局视觉意图 LLM 分类，confidence≥0.8 才判全局）、mergeHintSystemPrompt（合并主/次页+子页面名 LLM 抽取）、资源占位符保留规则） |
| 生成/编排逻辑 | `src/lib/pipeline/orchestrator.ts`（runPipeline（含 intent 意图识别阶段，分类器在 intent.ts/judges.ts）/runGenerate/runEdit/runRawHtml/runEditRawHtml（opts.applySelectedStyle 把已选风格套到原页编辑）/tryScopePatch/structureCheckStage/reviewAndRefine（**当前均临时整体跳过**、见 §6）/validateStageDetailed、MAX_FULLPAGE_EDIT_BYTES=DEFAULT_FULLPAGE_EDIT_THRESHOLD_BYTES=640000（服务端；桌面端 claude-html-utils.cjs/main.cjs 仍 160000 未对齐、见 §6）大文件闸口走 HtmlSizeInfo；detectScriptInjectedAnchor 命中脚本动态生成内容时跳过局部 DOM patch、补强 instruction 后走整页/Claude；isDeleteInstruction/isInteractiveHtmlEditInstruction；全局视觉编辑改由 classifyGlobalVisualEditInstruction（LLM，confidence≥0.8 才判全局）；directHideElement/tryDeterministicScopePatch 隐藏+确定性 color/attr/text 快路径；resolveInteractiveHtmlEditInstruction+classifyInteractiveEditIntent 交互意图；pointSelectEditModelOverride 点选编辑升模型；buildClaudeEditFocus 给桌面 bridge 的精确编辑锚点；repairUnsafeHtmlNavigation 原生 HTML 生成/整页编辑后的导航 LLM 定向修复（**当前临时停用**、见 §6）；checkInlineScriptsSyntax（textUtils，@babel/parser 逐段校验内联 script JS 语法）+ repairInlineScriptSyntax 生成后定向修复；runRawHtml 原样打开检出导航只提示不改文件；desktop-claude-progress 进度事件；纯函数 textUtils.ts（含 looksLikeHtml）、判别 judges.ts（含 planHtmlEdit（withTimeout 5s、EditPlan.operation 含 dedup）/classifyEditIntent/classifyInteractiveEditIntent/classifyGlobalVisualEditInstruction/extractMergeHints/locateScopeTarget/preflight）；校验/防幻觉 validate.ts（含 normalizeBrandColors 品牌色确定性矫正、repairIfNeeded 第 4 类导航修复项 + blockedNavigation 兜底；**注意**：validate.ts/structureCheck.ts 仍含受 useDpl=false 守卫的 DPL 校验分支，当前不可达，待清理见 §6）；tryScopePatch 接可选 editPlan，dedup operation 走 selectDedupScope、静态无重复升级整页 |
| 上传意图识别 | `src/lib/pipeline/intent.ts`（classifyHtmlUploadIntent HTML 上传 open/edit/regenerate/ask、classifyImageUploadIntent 图片上传 ask/generate/generate-with-changes、userContent 多模态组装、summarizeHtmlForIntent） |
| 模型路由启发式 | `src/lib/pipeline/routing.ts`（shouldUseStrongInitialGenerate/simpleGenerateOverride/strongRetryOverride/isRestorationRequirement/isDenseRequirement 强模型/评审风险启发式、inferDeviceByRule 快速图片模式规则判端、shouldSkipImagePlanning；从 orchestrator.ts 抽出） |
| 阶段耗时埋点辅助 | `src/lib/pipeline/timing.ts`（timingBase/recordTiming/timed/modelTiming 包装 recordStageTiming） |
| HTML 编辑体量口径 | `src/lib/pipeline/htmlSizeInfo.ts`（analyzeHtmlEditSize 同时给 originalBytes/compactBytes/assetCount/savedBytes/canFullpageEdit/shouldUseClaude/tooLargeForClaude） |
| Raw HTML 双表示 | `src/lib/pipeline/rawHtmlState.ts` + GenerationResult.rawHtmlState：code/preview.html 保留可渲染完整 HTML（previewHtml），rawHtmlState.editHtml 给模型/Claude/点选修改使用，rawHtmlState.assets 负责 __YD_ASSET_<hash>__ 回填。直接编辑预览会清空 rawHtmlState，下一次 AI 编辑再重建 |
| 原生 HTML 可视化直接编辑 | `src/components/DirectHtmlInspector.tsx`（单选元素属性面板）+ `src/lib/directHtmlEditor.ts`（跨 iframe realm 元素识别、属性级单位归一化、CSS.supports 校验、可编辑祖先路径、可逆事务、同字段连续输入合并、重置基线、会话 token 临时标记清理）+ `PreviewPane.tsx`；回归 `npm run test:direct-html-editor` |
| data URI 占位压缩 | `src/lib/pipeline/dataUriPlaceholder.ts`（compactDataUris/expandDataUris/hasDataUriPlaceholder/validateAssetPlaceholders，MIN_SAVING=2048，仅作用于送模型入参边界，绝不改 original） |
| 导出 HTML（离线内联）+ 改名 + 桌面静默下载 + 移动端窄框 | `src/lib/exportInline.ts`：inlineSandboxRuntime（历史 DPL 沙箱运行时内联，原生 HTML 路径无外链、直接用）；applyMobileNarrowFrame 在导出/分享时把整页装进手机宽度 iframe（仅当预览切移动端但生成结果实为 PC/抓取页时，真移动端跳过避免二次收窄）；injectHistoryBridge 把浏览器后退接到 iframe guard 的 __ydGoBack、让导出/分享链接支持系统返回回退多级页面（bridge 必须注入外层文档、不得注进 srcdoc 属性内）；调用点在 PreviewPane.tsx 的 buildExportHtml。导出/分享都先弹窗让用户确认文件名（exportName 输入框）。桌面端经 preload onExportSaved 收到主进程 will-download 静默保存路径后弹提示 |
| 上传附件 / ZIP 自包含转换 / 拖拽 | `src/app/page.tsx`（最多 4 个附件 MAX_ATTACHMENTS=4、输入框 drag/drop 拖拽上传、HTML 上传即走 rawHtml 路径无勾选框）、`src/app/home-helpers.ts`（HTML/ZIP/Word/文本读取；ZIP 资源内联；MAX_UPLOAD_MB=5 单文件上限；__bundler/* 单文件打包器解包）；送模型图片上限 MAX_IMAGES=4（src/lib/pipeline/constants.ts） |
| 原样 HTML 修改防白屏 | `src/lib/pipeline/orchestrator.ts`（tryScopePatch 局部优先 + 大文件闸口）、`textUtils.ts`（validateEditedHtmlDoc({deleteMode}) 完整 HTML 强校验 + 0.8 体量比、删除豁免）、`htmlScopePatch.ts`（点选修改使用的标注 anchor 局部作用域替换 / directDeleteElement 删除快路径 / directHideElement 隐藏快路径 / directDeleteColumn 整列跨行确定性删除 / tryDeterministicScopePatch color/attr/text 确定性替换 / validateScopeReplacement({deleteMode}) 删除放宽下限 / matchLocateToAnchor 多信号打分（offsetHint 优先精确定位）/ buildDomSummary 上限 30KB / isSemanticContainer 业务类名识别 / detectScriptInjectedAnchor 动态内容检测 / selectHtmlPatchScope（接可选 plan：{operation,scopeHint,batch}，plan 在场用 LLM 计划驱动页签/表格/删除移动/批量四类上提决策、plan 缺席回退原正则）/ selectDedupScope（点选去重） |
| 多 HTML 合并（程序化、不调 LLM 重写） | `src/lib/pipeline/orchestrator.ts`：合并主/次页 + 子页面名 + 形态先经 extractMergeHints（judges.ts，LLM best-effort 抽取，失败/超时回退正则，**合并本身仍程序化、不回退 LLM 重写**）；resolveMergeForm 按指令解析合并形态（drawer/modal/page/tab、缺省 drawer）、resolvePrimarySecondary 主次页判定、deriveExtractTarget 接 hints；programmaticHtmlMerge 抽屉/弹层形态；programmaticNavigationMerge 导航形态（指令含「新页面打开」）--点主页触发点全屏 iframe 打开整页次要页、支持多级合并 + 下钻树自动发现 + 编辑态增量嵌套；mergeFailureMessage 失败给人话原因**不回退 LLM、直接提示让用户修触发点/文件名/页面结构**；MAX_MERGE_HTML_UPLOAD_BYTES=10MB 按 UTF-8 字节计；合并形态分流在 runPipeline 两入口；回归 `npm run test:html-merge` |
| 原型真实导航拦截 | `desktop/prototype-navigation-core.cjs`（**Next 与 Electron 共用的唯一规则源**，`src/lib/prototypeNavigation.ts` 只是其薄封装 + prototypeNavigationRepairInstruction 修复指令；8 类模式：location 赋值/跳转 API/window.open/非锚点 href/form action/target _top·_parent/meta refresh/base href）、`src/lib/previewNavigation.ts`（预览期守卫 guardPreviewNavigation 注入 srcDoc / stripPreviewNavigationGuard 序列化剥除 / isExpectedPreviewDocument 检测 iframe 跳走）、接线：validate.ts（修复链第 4 类问题 + blockedNavigation 兜底页）、orchestrator.ts（repairUnsafeHtmlNavigation 原生 HTML 修复，**当前临时停用**、见 §6）、PreviewPane.tsx、desktop/main.cjs（Claude 结果不得新增真实导航）；回归 `npm run test:preview-navigation` |
| 产品风格档案 | `src/lib/style/profiles.ts`（8 个：Ant Design/TDesign/Vant/Apple/Claude/Notion/Slack/Vercel，各含 styleSpec+themeCss）；选中档案尚未生成时的**视觉预览卡**在 `src/components/PrototypeControls.tsx` 的 STYLE_PREVIEWS/StylePreviewCard（含 mobile 手机形态预览卡，Vant 使用）+ `src/app/globals.css` |
| 默认产品壳 pattern | `src/lib/style/patterns.ts`（buildStyleHead/matchStylePatterns；**原默认产品壳 pattern 已移除、只透传档案 styleSpec**）。如需自有产品壳，往 STYLE_PROFILES 加档案并在 patterns.ts 实现 pattern、structureCheck.ts 加校验 |
| 会话持久化（IndexedDB） | `src/lib/store/sessions.ts`、`src/components/HistoryDrawer.tsx`；会话级连续对话上下文在 `src/lib/pipeline/sessionBrief.ts`：GenerationResult.sessionBrief 随每个历史版本保存 SessionBriefV1（初始需求/澄清补充/初始 Prototype Contract/最近 6 条成功修改 + 最近 4 条版本级纯文本对话快照）。客户端恢复与 API 边界都先 sanitizeSessionBrief，旧版/损坏 Brief 清洗失败时安全重建。Prompt 按「最近对话→最新成功修改→初始合同→澄清补充→初始需求」分配 7000 字预算。历史上下文经转义后放在 user 消息的 <session_context_data> 数据块，本轮输入固定放在最后；system 只保留固定的优先级/不执行历史命令规则。仅作用当前会话/当前版本，不跨会话、不新增模型调用、不做用户记忆 |
| 需求评审卡 | 类型在 `src/lib/types.ts`（RequirementCard/RequirementCardLink/RequirementCardSet）；纯函数 `src/lib/requirementCardUtils.ts`；面板 `src/components/RequirementCardsPanel.tsx`；预览侧 `src/components/PreviewPane.tsx`（预览头「需求卡」开关、「关联区块」点选元素生成语义锚点 selector/domPath/text/role/rect、req-link-marker 覆盖层+hover 贝塞尔连线、导出/分享勾选「附带需求卡」经 buildExportHtml({includeCards}) 注入） |
| 前端 / 预览面板 | `src/app/page.tsx`（两栏/三栏布局切换 yd_layout_mode；loadingHint 预览骨架按模式/场景分级预估时长；dplLocked/styleLocked；输入框 drag/drop 拖拽上传；/api/generate 失败自动重试 GENERATE_RETRY_COUNT=2 次/间隔 500ms，仅流式 error 事件与网络异常重试、HTTP 错误响应与用户取消不重试；loading 期间 beforeunload 离开二次确认）、纯工具 `src/app/home-helpers.ts`、`src/components/PreviewPane.tsx`（预览视口设备模式 PreviewDeviceMode（auto/pc/mobile）与手机外壳 MobilePreviewShell（企微 390×844/苹果 393×852/安卓 412×915）；预览空态渲染 src/app/example-prompts.ts 的示例提示词卡片 prompt-grid，含 tag/标题/一键复制、每次渲染 Fisher-Yates 随机洗牌；iframe srcDoc 经 buildCapturedPagePreview + guardPreviewNavigation 注入临时导航守卫）、`src/app/globals.css`（全量改用 var(--brand) 等主题变量）；从 page.tsx 抽出的纯工具 `src/app/page-utils.ts`、桌面 Claude bridge 辅助 `src/app/desktop-claude.ts`（bridge 检测/日志路径解析/失败格式化/DESKTOP_CLAUDE_PROTOCOL_VERSION+desktopClaudeCompatibilityError 协议版本与能力集兼容性检查/sha256Text）、UI 组件 `src/components/PrototypeControls.tsx`（MODEL_PREFERENCE_OPTIONS/VISION_MODEL_PREFERENCE_OPTIONS/ModelPreferencePicker/StyleProfilePicker/StylePreviewCard/MsgContent） |
| UI 主题（应用外壳配色，≠ 产品风格档案） | `src/lib/theme/themes.ts`（5 套主题 CSS 变量：晴空蔚蓝风 classic/石磨复古风 shimo/夜幕苍穹风 yemu/和煦橙光风 nuancheng/浅滩椰林风 yecui，默认 classic）· `ThemeContext.tsx`（ThemeProvider/useTheme，把 vars 写到 :root + 设 data-theme，localStorage key yd_theme 持久化）· `src/components/MoreMenu.tsx`（预览头「更多」下拉：主题切换 + 退出登录）。**只影响外壳，不进生成管线、不注入 prompt** |
| Electron 客户端壳 | `desktop/main.cjs`（窗口、安全策略、Web URL、本机 server fallback、菜单、Claude IPC、desktop-attachment:open 附件打开桥、启动清 24h+ 残留、isTrustedAppUrl 校验）、`desktop/preload.cjs`（sandbox=true 下只暴露最小 bridge：openAttachment/getClaudeStatus/runClaudeHtmlEdit/cancelClaudeHtmlEdit/openConfigFolder/sha256Text/onExportSaved/revealExportFile）、`scripts/prepare-desktop-standalone.mjs`（desktop:build 调：把 .next/static+public/ 拷进 standalone、剥掉 data/tmp/vendor/.env）、package.json 的 desktop:* 脚本与 build 配置（output:standalone 在 next.config.mjs）；从 main.cjs 抽出的辅助模块 `desktop/desktop-utils.cjs`（byteLength/isExecutable/normalizeBaseUrl/parseEnvFile/safeReaddir/sleep/sha256Text/terminateChildProcessTree 进程树终止——POSIX 用 detached 进程组 kill(-pid)、Windows 用 taskkill /T /F）、Windows 平台层 `desktop/windows-runtime-utils.cjs`（where.exe+PATH/PATHEXT 探测 claude.exe/claude.cmd、Git Bash 发现、受控 cmd.exe 启动）、`desktop/claude-runtime-utils.cjs`（ClaudeJobQueue 串行队列：按 jobId enqueue/cancel/waitForIdle、每任务 onCancel/throwIfCancelled、writeClaudeJobStatus/cleanupClaudeJobDirs）、`desktop/claude-html-utils.cjs`（buildRelevantHtmlSnippets/compactDataUrisForClaude/validateClaudeHtml/analyzeHtmlSizeForClaude/analyzeScopedClaudeSafety/buildTabCompletionContract）、`desktop/claude-fragment-utils.cjs`（多片段事务：提取主 HTML/overlay/CSS/handler/数据源片段、≤8 片 ≤320KB 不重叠、逐片校验后按 offset 倒序原子回填）、`desktop/prototype-navigation-core.cjs`（+.d.ts，导航守卫规则唯一实现，Next prototypeNavigation.ts 与 Electron 共用）、`desktop/attachment-utils.cjs`（sanitizeAttachmentName/bufferFromIpcBytes/htmlWithAttachmentCsp）、`desktop/capture-payload-utils.cjs`（normalizeCapturePayload/normalizeGuidedTabCapture+CAPTURE_IMPORT_MAX_BYTES）；Windows x64 NSIS 薄包配置 electron-builder.thin.win.yml；导出 HTML 走 <a download> 触发下载时，session.defaultSession.on("will-download") 拦截、item.setSavePath 静默存到下载目录 |
| 桌面 Claude Code bridge | 唯一触发协议：Web 与 Electron 内本机 Next server 都在大 HTML 普通对话框编辑或点选修改兜底时发 desktop-claude-required（携带 editHtml+assets+instruction+focus），前端 src/app/page.tsx 先用 getClaudeStatus 强校验 bridge v5 + 必需 capabilities 再调 runClaudeHtmlEdit；只有 desktop/main.cjs 的 ClaudeJobQueue 可串行拉起 Claude CLI。buildClaudeEditFocus 提前算编辑计划+DOM 锚点；prepareScopedClaudeJob scope 自包含时收窄为单容器；命中 scope 外 handler/drawer/modal/CSS/JS/数据源时由 desktop/claude-fragment-utils.cjs 做多片段事务；页签类 scoped 任务另带 tabCompletionContract；batch=yes/needsFullPage=yes、全局主题/布局/响应式、歧义/过大仍走完整页。完整页把无关 DOM 子树/CSS/JS 变成带 hash 不可变占位，renderer 只传资源占位 HTML+资源表+完整页 SHA256、Electron 原样重建避免 IPC 双份 HTML；desktop/prototype-navigation-core.cjs 是 Web/Next/Electron 共用真实导航唯一规则源。**no-op 出口、页签守卫、scope 拒绝、复合筛选表多片段、表头校验、串行排队按 jobId 取消、日志/任务清理、manifest.json 真源、TASK.md 只留索引**等机制见 §4 对应铁律；CLAUDE_AGENT_* 旋钮只由 Electron 配置读取、不进 Next config.ts |
| 浏览器抓取扩展 | `extension/youdesign-capture/`（Chrome MV3，**v0.2.9**，权限 activeTab/scripting/storage/tabs/webNavigation，**host_permissions 为 `<all_urls>`、content_scripts 在所有 http(s) 页 document_start 注入 drawer_tracker.js** 做加载期点击跟踪）：action 带 default_popup（popup.html+popup.js），两入口--「抓取当前页」直接 captureAndSend、「多页签采集」注入 capture_overlay.js 浮窗走引导式采集。**富抓取**（captureRenderedPage，注入前校验 URL 是 http(s)://、拒绝 Chrome 内部页/扩展页）：document.documentElement.cloneNode、同步表单状态、markOpenDrawers 标记可见抽屉/模态框、markPreloadedTabs 标记已在 DOM 的标准/Ant Design 页签、freezeFrameRefs 冻结 iframe、内联可读 CSS（styleSheets+adoptedStyleSheets、@media/@supports/@layer/@container 嵌套、url()/@import 绝对化）、captureOpenShadowRoots 用声明式 shadow DOM、canvas toDataURL 快照、src/href/srcset 绝对化、snapshotCanvases、disableScriptsAndHandlers（script 改 type=text/plain、去 on*/javascript:）、sanitizeCapturedDocument（清 Plasmo plasmo-csui/Google 翻译 goog-gt-*/广告遮罩/扩展协议资源 + 移除所有 base 标签）、normalizeCapturedDocument（createHTMLDocument 重建标准骨架）、addCaptureMetadata；产出 captureMeta schemaVersion 2：interactions.drawers/tabs、frames（captureChildFrames 用 allFrames+webNavigation.getAllFrames 抓子 iframe，depth ≤3、≤12 帧、无单帧 1MB 限制、映射须唯一否则 unavailable/omitted）、resources、guidedTabs。远程 CSS 内联 ≤4MB（单 ≤2MB）、远程图片内联 ≤900KB（单 ≤256KB、≤32 张）；总产物 ≤5MB。**污染 canvas 截图兜底 recoverTaintedCanvases**：对抓取瞬间可见的污染 canvas 用 chrome.tabs.captureVisibleTab 截整张 tab 可见区、按 canvas 视口坐标 OffscreenCanvas+createImageBitmap 裁出 PNG data URL 贴回；不可见/越界/失败退回诚实占位。drawer_tracker.js：监听 pointerdown/click+MutationObserver，按 observed-click 或 semantic（aria-controls/data-target/href=#id）把抽屉映射到 opener，fingerprint 在重渲染后重定位。引导式多页签采集（capture_overlay.js 浮窗 + service_worker handleGuidedCaptureMessage）：inspect-tabs 发现标准/Ant Design 页签组（2-16 个 tab）、start-guided 以开始时 DOM 为基线（skipPreloadedTabs）、capture-guided-tab 克隆当前面板、finish-guided 合并。投递：优先 POST http://127.0.0.1:17631/capture/import（x-youdesign-capture 头）给桌面 capture server，桌面不可用回退 YouDesign Web 的 window.postMessage（YD_CAPTURE_IMPORT/ACK），youdesign://capture 仅拉起/聚焦 app 不传数据 |
| 抓取页离线重建 | `src/lib/capturedPage.ts`（buildCapturedPagePreview/buildCapturedPageAttachment，仅对 captureMeta.schemaVersion===2 或含 data-yd-capture-* 标记的抓取页执行）：sanitizeSourceExecutableNodes（删 script/base/object/embed + on* + javascript:）、removeLiveResources（删外链 stylesheet、http 图片）、rebuildGuidedTabs、嵌套 iframe 递归 srcdoc 重建或 framePlaceholder 占位、注入 captured-page-runtime.cjs 的交互 runtime + 隐藏态样式。`desktop/captured-page-runtime.cjs`（CAPTURE_RUNTIME_SOURCE/captureRuntimeScriptTag）：**抓取页唯一允许执行的自有 runtime**，抽屉 open/close + 父子级联 + mask + opener aria-expanded + 焦点回 opener、页签 activate + 键盘导航、syncScrollState；Web 预览与桌面附件共用此字符串，桌面端对它算 CSP sha256 hash。`desktop/attachment-utils.cjs` 的 htmlWithAttachmentCsp：检测 data-yd-capture-drawer/tab 标记时 script-src 放行 sha256-<runtime hash>、否则 'none'；allowCapturedFrames 时只保留构建器生成的静态 srcdoc iframe、其余 iframe 一律替换占位 |
| LLM 适配器 | `src/lib/providers/`（anthropic 含 stream() 流式，Opus 5/Sonnet 5 显式 thinking:{type:"disabled"} 关闭其默认 adaptive thinking；openaiCompatible 记录 stop_reason/finish_reason；Kimi K3 经火山方舟 Agent Plan（Anthropic 兼容 /v1/messages + Bearer）/ openaiCompatible(含流式，含 GLM-5V 视觉) / mock）；视觉模型选择在 providers/index.ts（needsVision 时尊重前端显式选 kimiK3/glm5v/sonnet/opus，未指定默认 glm5v）；文本模型亦可选 kimiK3/sonnet/opus（TextModelPreference） |
| 临时分享隧道 | `scripts/tunnel.mjs` + package.json 的 npm run tunnel（cloudflared trycloudflare 快速隧道） |
| 多用户认证 | `src/lib/auth/`（passcode/session/users）、`scripts/user.mjs`（CLI 管户）、`src/app/api/{login,me}/route.ts`、`src/middleware.ts`（验签 cookie） |
| 用量计量 | `src/lib/meter/`（types/prices/context/mysql/store/jsonl/meteredProvider）、provider 的 onUsage 回调、`src/app/api/generate/route.ts` 的 runWithMeter；明细写 MySQL usage_records 或回退 JSONL（<data dir>/usage.jsonl），连接只从 YOUDESIGN_MYSQL_* 环境变量读，价格表 data/prices.json 可覆盖默认刊例价 |
| 用量看板 | `src/app/usage/page.tsx`、`src/app/api/usage/route.ts`（MySQL 直接聚合或 JSONL 聚合，按人/模型/中国自然日）；连通与写读回滚验证 `npm run test:mysql-usage`、`npm run test:usage-jsonl` |
| 阶段耗时埋点 | `src/lib/meter/stageTiming.ts`（recordStageTiming 落 data/stage-timing.jsonl + 进程内 group-by 聚合日志）+ types.ts 的 StageTimingRecord；orchestrator.ts 在各阶段记耗时；维度含 stage/model/modelKey/mode/artifact/useDpl/rawHtml/hasAttachments/hasImages/hasDocs/fastMode/device/outcome；data/stage-timing.jsonl 仍是本地运行时日志、不入 MySQL |
| 其它手动自测脚本 | `scripts/` 下若干一次性/场景化手动脚本（test-annotation.mjs、test-iter-edit.mjs 等），多为联调期临时产物、需本地服务与密钥，不保证随代码演进维护；正式回归以 package.json 的 test:* 为准 |

## 4. 踩坑铁律（务必遵守）

- **device（pc/mobile）贯穿分流**：⓪ 预检从需求文字自动判定（手机/H5/App/小程序→mobile；后台/管理系统→pc；判不准则反问；兜底 pc），贯穿 generateSystemPrompt/validateStage。
- **生成代码是原生 HTML**（内联 CSS/JS、原生 JS 做多页切换、自包含可离线打开）。
- **`src/instrumentation.ts` 必须保持空钩子**：Next 启动时会编译 instrumentation 的 register()；在里面预热 embeddings 会把 child_process/node:* 等 server-only 依赖带进编译链，导致 dev server 首页 500。这类依赖只在真实请求路径里按需惰性加载。
- **tsconfig 必须 exclude: ["node_modules","vendor","scripts"]**。
- **npm 安装**：公网 registry；带原生二进制 postinstall 的包（esbuild）若走代理需显式 --proxy/--https-proxy。
- **网络**：`src/lib/http.ts` 的 httpFetch 自行解析 NO_PROXY（命中直连、否则走 HTTPS_PROXY 代理）。httpFetch 重试两类**可恢复**失败（maxRetries=3）：① 网络层抛错（fetch failed / other side closed 等代理抖动，未拿到响应、安全幂等）短退避 0.5s/1s/1.5s；② 过载/网关响应 429/502/503/504 退避 1s/3s/9s（≈13s 上限）、尊重 Retry-After 头（单次封顶 9s）。**500 与其它 4xx/5xx 不重试**，原样返回交调用方。POST LLM 调用幂等（同 prompt 重发不影响结果正确性），重试安全。gh/git 走公网偶发 HTTP2 瞬断——push / gh pr create|merge 报错时先 gh pr view 看真实状态（往往服务端已成功），失败就重试，再 git fetch 同步本地，别重复造分支/提交；git config http.version HTTP/1.1 可缓解。
- **env 缺失自动回退 mock**；密钥都通过 config.ts 读，不要硬编码。`.env.local` 绝不提交；`.env.example` 是**占位符模板**（无真实凭据，已入库），新环境参考 README 配置表填真实 key。
- **Electron 桌面版默认是 Web 壳，不是本机服务**：打包态默认加载 http://localhost:3000/youdesign（假定用户自起 Web 服务），复用 Web 登录、模型配置、会话接口和用量体系。只有 YOUDESIGN_DESKTOP_USE_LOCAL_SERVER=true 才启动 .next/standalone/server.js；默认地址可用 YOUDESIGN_DESKTOP_WEB_URL 覆盖。不要再恢复首次启动"创建本机用户口令"的 setup 页面——客户端登录直接走 Web 登录；本机 server fallback 才强制 YOUDESIGN_AUTH_MODE=shared。
- **Electron preload bridge 只做最小授权**：renderer 保持 nodeIntegration=false、contextIsolation=true、sandbox=true，preload 只暴露 openConfigFolder/getClaudeStatus/runClaudeHtmlEdit/sha256Text 等窄接口。**sandbox=true 下 preload 只能 require electron 白名单（electron/events/timers/url），不能 require 本地文件或 node:crypto/node:fs 等 Node 模块**。需要 Node 能力的重活（如 SHA-256）一律走 ipcRenderer.invoke 交主进程处理，主进程的 ipcMain.handle 必须校验 sender URL 属于当前可信 Web base（isTrustedAppUrl），外部导航一律拦截并交给系统浏览器。
- **Claude CLI 路径不能只信 GUI App 的 PATH**：macOS 双击 Electron App 常拿不到用户终端里的 npm/nvm PATH。desktop/main.cjs 的 resolveClaudeCliPath 必须探测 zsh login/interactive shell、~/.claude/local、Homebrew、npm global、nvm/fnm/volta/asdf 等常见安装点；CLAUDE_CLI_PATH 若不可执行要自动回退。
- **桌面 Claude Code 只有一条执行路径**：orchestrator.ts 在大 HTML 普通对话框编辑超阈值时发 desktop-claude-required，点选修改先做 scope patch、兜底才发事件；脚本动态生成元素会补强 instruction 后 handoff。src/app/page.tsx 收到后先 getClaudeStatus，再通过 preload 调 runClaudeHtmlEdit。Next server 禁止 spawn Claude CLI，禁止恢复 claudeHtmlAgent.ts/YOUDESIGN_DESKTOP_AGENT 分流或 /api/desktop/claude/cancel。任务只限 job 目录内改 index.html；完整页与投影任务的 TASK.md 必须从实际发送的 HTML 保留 Relevant HTML Snippets；已物化 scope/投影/多片段上下文时不得再次嵌入 target/scope HTML，多片段的权限、依赖、限制与 Contract 以 manifest.json 为唯一真源，TASK.md 只保留索引和计数。结果由 desktop/claude-html-utils.cjs 统一执行资源/不可变占位、文档完整性、空改、重写、交互增量和删除脚本引用守卫。**Claude CLI 使用 --permission-mode bypassPermissions+--tools LS,Bash，必须靠 TASK.md constraints 约束 Bash 仅限本目录只读**。
- **Claude no-op 必须有澄清/已满足两条出口、不能伪装成成功**：Claude 流末若 isTrivialNoOp(html, edited) 命中（index.html 未改），不得当成功返回原页——main.cjs 用 extractClaudeClarification/extractClaudeAlreadySatisfied 从流末 summary（截断 1600 字符）解析：优先认 YD_NEEDS_CLARIFICATION:/YD_ALREADY_SATISFIED: 显式标记，无标记时按澄清信号（"需要确认/请补充/scope 不匹配/无法定位"+问号）或已满足信号（"already satisfies/已满足/无需修改"+"left index.html unchanged"）兼容识别；命中分别写 needs_input/no_change job 状态、返回 {ok:false,needsClarification,clarification}/{ok:false,alreadySatisfied,message}，前端生成对应 assistant 消息并入版本快照、不产新版本。**用户明确否定当前效果**（"没改好/未生效/不对/重新修改"）时不得判成已满足。改这条要同步 claude-html-utils.cjs 的两个 extractor + main.cjs 的 no-op 分支 + 前端 page.tsx 的 !bridge.ok 处理 + test:desktop-claude-core 的两条出口用例。注意：这是**桌面 Claude 路径**的 no-op 出口、仍生效；服务端 tryScopePatch/runEditRawHtml 的 isTrivialNoOp+hasInteractionDelta 闸门是另一套、当前临时屏蔽（见 §2 ⑤ 与 §6）。
- **页签类任务必须有自包含 JS 守卫、不能只改静态 active class**：指令含「页签/标签页/tab」+「点击/切换/新增/展示…」时 main.cjs 的 buildTabInteractionSection 在 TASK.md 注入页签交互要求——保留原活动页签、所有现有页签保持可点击、加自包含 JS（点击同步 active class/icon 色/aria-selected/匹配 tabpanel active+aria-hidden/ink-bar 位置）、handler 限定当前页签组。validateClaudeHtml 交互类编辑用 hasInteractionDelta（handler 块集合比对）+ isTabSwitchInstruction&hasTabSwitchImplementation（页签类必检可执行点击切换逻辑）。buildClaudeEditFocus 的 claudeFocusScopeMismatchReason 对页签触发+面板跨区域（tab_trigger_and_panel_cross_region）拒绝下放 scope、退回完整页；**但 scope 已含完整页签组**（同时有 role=tab+role=tabpanel）时不再判跨区域拒绝。页签类 scoped Claude 任务另带 tabCompletionContract：校验页签/面板数量、原页签文本全保留、新增页签文本、默认激活页签、内容词、可执行点击切换，缺任一项返回「客户端增强未完成」并保留原页。
- **关闭客户端要释放 Claude CLI 占用**：只有 desktop/main.cjs 可以跟踪并拉起 Claude 子进程；必须在 SIGTERM/SIGINT/beforeExit/窗口关闭时清理，避免占用本机 Claude Code 网关影响 VS Code Claude 插件。
- **Claude 任务必须串行排队且按 jobId 可取消**：desktop/main.cjs 用 ClaudeJobQueue 串行化 desktop-claude:edit-html；取消只走 desktop-claude:cancel-current(jobId)，排队任务立即移除，运行中任务只取消自己，空闲取消不得污染下一任务。Claude CLI 以独立进程组启动，取消/超时/提前结束先发 SIGTERM，2s 后对整个进程组兜底 SIGKILL。前端停止按钮 abort /api/generate 并调用 Electron cancel bridge。getClaudeStatus 返回 bridge v5、capabilities、busy/running/queueSize；前端必须校验 prepared-html、raw-html-state、precise-focus、strong-validation、electron-only-executor、job-scoped-cancel、reconstructed-html-input、sha256-bridge。DMG 默认 HTTP Web 不具备 crypto.subtle，必须由 preload 窄授权 hash bridge 提供 SHA-256。不要绕过队列直接 spawn Claude CLI。
- **模型 API 重试必须有独立无进展熔断、预算触顶走结果抢救**：Claude CLI 流出现 system/api_retry 后默认 180 秒没有恢复到 assistant/user/result 事件就终止进程树，避免一直耗到 10 分钟总超时。**预算触顶**（error_max_budget_usd/budget_exhausted/Reached maximum budget/达到预算上限，CLAUDE_AGENT_MAX_BUDGET_USD 默认 $2）与达到最大轮次一样**不直接失败**，而是改走「校验已写入结果」分支：消息提示「Claude Code 达到预算上限/达到最大轮次，正在校验已写入结果」，继续过 scope/多片段原子回填、完成清单、导航和 Prototype Contract 校验，全部通过才应用抢救结果。总超时或重试熔断前若任务文件已经写入，不得直接丢弃；零写入要明确报告"模型尚未开始写入"，部分写入由原子校验拒绝并保留原页面。
- **Claude 日志可视化是窄授权**：Electron bridge 失败时把本次 claude-stream.jsonl 路径通过隐藏 marker 带回前端，ChatMessage.claudeLogPath 渲染"查看日志"按钮；desktop-claude:open-log 必须校验 sender URL 可信、路径在 Claude jobs 目录内且是 .jsonl，只用系统默认应用打开日志，不开放任意文件/目录能力。
- **跨筛选区与表格的复合任务不得把轮次耗在逐行探索**：desktop/main.cjs 必须从发送给 Claude 的实际 HTML 计算 Authoritative Table Structure，此类任务的 --max-turns 至少为 28。单行压缩表格允许 Claude 用一次本地 node -e 仅读写 manifest 声明的可写表格 fragment 做位置化批量变换；达到轮次上限且文件未改变时直接报告"达到最大轮次、尚未写入"。
- **表头完成度校验不能假设目标表格是页面第一张表**：hasExactHeaderCellText 必须遍历全部 thead；命中目标表头后，宽表几何和姓名（手机号）模拟数据校验必须继续锁定同一张表及其相邻分离式 body table。
- **可安全识别的跨筛选区与表格任务优先走复合多片段事务**：prepareMultiFragmentClaudeJob 对普通 batch=yes/needsFullPage=yes 仍拒绝，但对能唯一锁定筛选 label 容器、目标 th、列数一致的 tbody，以及表头/表体安全共同容器的复合任务，提取空的 filter-control-insert 可写插入片段、filter-anchor-context 只读原筛选项和 table-region 可写表格片段。两个 required 可写片段必须同时修改；完成度校验逐行核对最终列数、原行数和每行目标格式，只有两个片段全部通过才允许提前结束并原子回填。锚点重复、各行列数不一致、片段重叠或总量超限必须退回完整页。
- **浏览器抓取扩展走 HTTP localhost、不走 youdesign:// 传数据**：extension/youdesign-capture/（MV3，v0.2.9，host_permissions 为 `<all_urls>`、content_scripts 在所有 http(s) 页 document_start 注入 drawer_tracker.js 做加载期点击跟踪）捕获已登录页渲染后整页 DOM（禁用所有 script、canvas 快照、内联 CSS、远程 CSS/图片转 data URL、5MB 上限，schemaVersion 2 的 captureMeta 含 drawers/guidedTabs/frames）后，**数据走 POST http://127.0.0.1:17631/capture/import**（x-youdesign-capture 头），youdesign://capture 协议 URL **只用于拉起/聚焦 app、不传数据**。startCaptureImportServer 必须校验 origin 是 chrome-extension:// 且带 x-youdesign-capture 头、CAPTURE_IMPORT_MAX_BYTES=6MB 限体积。桌面不可用时扩展回退 YouDesign Web 的 window.postMessage（YD_CAPTURE_IMPORT/ACK）。不要把 capture 数据塞进 youdesign:// URL。
- **抓取页离线重建铁律（capture v0.2.9，schemaVersion 2）**：抓取页的离线交互（抽屉开关/页签切换/键盘导航）**只由 desktop/captured-page-runtime.cjs 这一份自有 runtime 驱动**，来源页脚本永不进入；桌面附件 htmlWithAttachmentCsp 用 script-src 'sha256-<CAPTURE_RUNTIME_SOURCE 的 hash>' 仅放行它（无 data-yd-capture-* 交互标记时 script-src 'none'）、frame-src 'none'、其余 script 一律 type=text/plain 失活。**改 runtime 源码必须同步重算 hash**，否则 CSP 拦截交互。buildCapturedPagePreview/buildCapturedPageAttachment 对 schemaVersion===2、含 youdesign-captured-from meta、或含 data-yd-capture-* 标记的抓取页执行，**普通上传 HTML 走原路径、不被改写**。**引导式页签采集必须以开始时的 DOM 为基线**（start-guided 先 capturePagePayload({skipPreloadedTabs})）。页签 key 必须按组传递稳定身份（groupId:tab-N），**绝不用扁平序号做全局 key**。离线重建删除未采集面板后，DPL/Ant 动画内容轨道的 margin-left/transform/translate 必须清零。**桌面附件不得保留来源 iframe**，只接受构建器生成的静态 srcdoc iframe（且无 live src），其余一律替换占位 section。逐页签采集必须轻量：只克隆当前已打开面板，最终 finish-guided 合并才离线化。子 iframe 抓取 depth ≤3、≤12 帧、单帧 html 无 1MB 固定上限（按 5MB 抓取总预算 / 6MB 导入上限决定是否保留）、映射须唯一否则 unavailable/omitted 占位；导入端 normalizeCapturePayload 必须转发扩展产出的 sourceUrl/frameName。离线产物剥所有 script/base/object/embed、on* handler、javascript: URL、外链 stylesheet、http 图片。改这条链路同步 capturedPage.ts/captured-page-runtime.cjs/attachment-utils.cjs/capture-payload-utils.cjs + 四个 test:capture-*/test:captured-page-* 回归。
- **多 HTML 合并是程序化的、不调 LLM 重写、失败不回退 LLM**：编辑态/生成态上传多个 HTML 时，先经 extractMergeHints 抽主/次页+形态+子页面名（失败回退正则）；programmaticHtmlMerge/programmaticNavigationMerge 程序化合并。**触发点按次要页文件名 stem 在主页及已嵌入页匹配**，找不到则 mergeFailureMessage 给人话原因**直接提示让用户修触发点/文件名/页面结构、不回退 LLM 重写**。MAX_MERGE_HTML_UPLOAD_BYTES=10MB 按 UTF-8 字节计。编辑态上传 HTML 但指令非合并（EDIT_MERGE_KEYWORD=/合并|嵌入|下钻页|并入|合到一起|并进来|打开/ 不命中且 editMergeHints.isMergeRequest 不为 true）则 strippedEditHtml 剥离 HTML 不转发 + editUploadNotice 阻断提示。改合并链路同步 orchestrator.ts 的相关函数 + test:html-merge。
- **导出/分享改名与桌面静默下载**：导出/分享都先弹窗确认文件名（exportName），改名持久化到会话 exportTitle、**不同步会话标题 customTitle**。桌面端 session.defaultSession.on("will-download") 拦截 <a download> 触发的下载、item.setSavePath 静默存到下载目录（**不弹系统「另存为」框**），成功后 youdesign:export-saved 推保存路径、前端弹「已保存到下载目录」+「打开文件夹」。改这条同步 PreviewPane.tsx（exportName/exportSavedPath/onExportSaved）、desktop/main.cjs（will-download）、desktop/preload.cjs（onExportSaved/revealExportFile）、sessions.ts（exportTitle）。
- **导出移动端窄框用 iframe 不用 body{max-width}**：PreviewPane.buildExportHtml 在 mobile && automaticDevice !== "mobile" 时调 exportInline.ts 的 applyMobileNarrowFrame 把整页 HTML 装进手机宽度的 srcdoc iframe 再导出/分享。**必须用 iframe、不能用 body{max-width:390px}**：抓取的真实移动端页面大量用 vw/position:fixed/@media(min-width) 等视口相关 CSS，按浏览器真实视口计算，body{max-width} 管不到；iframe 内部是独立 390px 视口，vw/fixed/media 全按手机宽渲染。**真移动端原型**（automaticDevice==="mobile"）必须跳过避免二次收窄。改这条同步 exportInline.ts 的 applyMobileNarrowFrame + injectHistoryBridge + PreviewPane.tsx 的 buildExportHtml 触发条件。导出窄框另注入 injectHistoryBridge：iframe guard 发 yd-nav-change 时外层 history.pushState、外层 popstate（浏览器后退/移动端手势/企微 webview 返回）调 iframe 内 __ydGoBack；bridge 必须注入外层文档、不得注进 srcdoc 属性内——原型自带 </body> 排在外层 </body> 前，正则匹配首个 </body> 会误注进 iframe，回归 npm run test:export-history-bridge。
- **桌面包型固定分两类**：薄客户端包只加载 Web + 本机 Claude bridge，不应包含 .next/standalone、大体积服务端依赖或源码私密文件；开发/应急胖包才保留本机 Next standalone fallback。打薄包固定用 npm run desktop:dmg:thin:mac（配置 electron-builder.thin.yml）。公网下载 Electron 资源失败时按需加代理：HTTPS_PROXY=<代理地址> HTTP_PROXY=<代理地址> npm run desktop:dmg:thin:mac。
- **薄包签名不能跳过**：electron-builder.thin.yml 必须保留 mac.identity: "-" 显式 ad-hoc 签名、hardenedRuntime: false。不要写 sign: false 或只依赖 CSC_IDENTITY_AUTO_DISCOVERY=false 跳过签名；未完整签名的 .app 会出现 Info.plist=not bound、Sealed Resources=none，安装后 macOS 常报"应用已损坏，应移到废纸篓"。打完必须验：codesign --verify --deep --strict --verbose=4 dist-desktop-thin/mac-arm64/YouDesign.app；hdiutil verify dist-desktop-thin/YouDesign-*-thin-mac-arm64.dmg；test ! -e dist-desktop-thin/mac-arm64/YouDesign.app/Contents/Resources/app/.next && test ! -e dist-desktop-thin/mac-arm64/YouDesign.app/Contents/Resources/app/node_modules。正式分发仍需 Developer ID 签名 + notarization。若本机安装仍被 quarantine 拦截，可执行 xattr -dr com.apple.quarantine /Applications/YouDesign.app。
- **DeepSeek deepseek-v4-pro 是推理模型**（max_tokens 同时计推理，要给足，否则正文截断；首字节慢 ~90s）；deepseek 路由使用 deepseek-v4-flash 并显式 thinking: disabled 保持快速非推理语义。**有图片的环节需视觉模型**：前端有图时模型选择器切到视觉列表，可显式选 GLM-5V（智谱 glm-5v-turbo，复用 GLM_API_KEY/ZHIPU_API_KEY）/ Kimi K3（火山方舟 Agent Plan，Anthropic 兼容端点 kimi-k3）/ Sonnet（Claude claude-sonnet-5）/ Opus（Claude claude-opus-5），**未显式选时默认 GLM-5V**。Sonnet 与 Opus 是用到 Claude（Anthropic 原生）的两个场景；Opus 5 / Sonnet 5 省略 thinking 时默认开 adaptive thinking，AnthropicProvider 对两者显式传 thinking:{type:"disabled"} 关闭。thinking enabled 时不传 temperature；Kimi K3 经 anthropic provider 走 /v1/messages、auth=bearer，同样不传 temperature；火山方舟 kimi-k3 默认开 thinking 且无预算上限、会挤占 max_tokens 致长 HTML 截断，AnthropicProvider 显式传 thinking:{type:"disabled"} 关闭。GENERATE_MAX_TOKENS=128000；EDIT_MAX_TOKENS=128000。
- **GLM 走智谱官方 OpenAI-compatible**：GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4，provider 拼 /chat/completions。
- **快速模式默认开（快速模式为默认）**：fastMode 独立于模型选择，只在首次生成前可切换；默认 true（前端 page.tsx 三处都默认 true：useState(true) 初始值、applySession 的 rec.fastMode ?? true、newSession 的 setFastMode(true)）。后端兜底仍 false（api/generate/route.ts 的 body.fastMode === true、orchestrator.ts 的 input.fastMode ?? false），但前端总会显式传 fastMode、兜底不触发。fastMode=true 时 structure/generate 强制 deepseek-v4-flash、跳过通用 review/refine；关时按 §4「模型选择」路由走 pro/GLM 并跑结构自检 + 自评审。**快速图片模式**（fastMode && hasImages && !hasDocs）更进一步跳过 clarify/structure 的 LLM 调用，inferDeviceByRule 规则判端 + 单页直出（timing rule-skip），省两次模型调用。改前端默认要同步这三处 + 文档。
- **登录后开新会话，不恢复上次**：login/page.tsx 登录成功时设 sessionStorage.yd_fresh_login=1 再跳转；page.tsx mount 时读该标记并清掉，**有标记则跳过 loadSession 恢复、直接开新会话**，普通刷新无此标记则正常恢复上次现场。换人/换班不互看草稿。
- **模型选择**：前端文本模型可选 默认模型（auto）/ kimi-k3 / DeepSeek / GLM-5.2 / Sonnet / Opus；有图片附件时模型选择器切到视觉列表（GLM-5V / kimi-k3 / Sonnet / Opus，默认 glm5v）。当前分流（关闭「快速」=高质量模式）：默认模型（auto）按 .env.local 的 ROUTE_*；DeepSeek 用 flash+pro 组合；GLM-5.2 把生成/编辑等主体交给 glm-5.2；kimi-k3/Sonnet/Opus 显式选时各阶段都走该模型（kimi-k3 走火山方舟 Agent Plan、Sonnet 走 claude-sonnet-5、Opus 走 claude-opus-5，均经 Anthropic provider）；simpleGenerateOverride 对 sonnet/opus/kimiK3 返回 undefined，简单原生页 flash-first 不触发、尊重用户选择。**自评审按风险分级**（fastMode=false 时）：结构自检已发现问题->直接 refine；低风险->跳过；仅风险信号才跑 flash 评审（当前临时整体跳过、见 §6）。
- **embedding 未计量**（成本占比极小且仅语义 RAG 开启时），待补。embedding 适配器在 src/lib/embeddings/（智谱 embedding-3 + 本地 n-gram 兜底），可复用。
- **原生 HTML 流式阶段不实时刷新预览**：code-delta 只更新代码区，预览等最终 code/preview/done 后一次性渲染，避免半成品 HTML 导致 iframe 高频重载和页签抖动。
- **原型严禁真实页面导航（srcDoc 会加载宿主应用）**：预览 iframe 与宿主同源，原型里任何真实 URL 跳转都会把 YouDesign 自己加载进预览区，形成「YouDesign 套 YouDesign」的嵌套页面。三层防线缺一不可：① prompts.ts 四个生成/编辑系统提示都含「严禁真实页面导航」规则；② 服务端静态门禁 unsafePrototypeNavigation（8 类模式，**规则唯一实现是 desktop/prototype-navigation-core.cjs**，Next 端 prototypeNavigation.ts 与 Electron Claude bridge 共用——改规则只改这一处）——修不掉返回兜底页 + blockedNavigation（runEdit 识别后保留原页）；原生 HTML 在 generate/runEditRawHtml 完成后**原设计**走 repairUnsafeHtmlNavigation 一次 LLM 定向修复；**当前临时停用**（见 §6），仅靠③预览期守卫兜底；上传原样打开**不擅自改文件**、只发提示；③ 预览期守卫 previewNavigation.ts 只注入 iframe srcDoc，**导出/保存/送模型/点选序列化必须 stripPreviewNavigationGuard 剥除**。改这套链路跑 npm run test:preview-navigation。
- **上传附件上限是 4 个**：图片/HTML/ZIP/Word/Markdown/文本合计 4 个（MAX_ATTACHMENTS=4）；生成态与原生 HTML 编辑态均开放"+"上传、粘贴、拖拽三种手势（编辑态 accept 与 editUploadFilter 仅放行 image/* + .html/.htm/.zip），单文件 MAX_UPLOAD_MB=5；送入视觉模型的图片另受 MAX_IMAGES=4 截断。
- **原生 HTML 编辑态图片送视觉编辑模型**：原生 HTML 产物修改轮上传的图片（≤4），前端 forwardImages 在原生 HTML 编辑态为真、requestUsesVisionModel=forwardImages 把 modelPreference 升为视觉模型随 /api/generate 请求体送后端；后端 runEditRawHtml 经 opts.images 接收，**仅整页编辑路径**单独取视觉 provider 并用 userContent(message,images) 拼多模态消息送图；tryScopePatch/tryDeterministicScopePatch/buildClaudeEditFocus/repairUnsafeHtmlNavigation 仍用文本编辑模型、不送图。
- **附件发送后回显 + 可点击打开**：用户气泡下方渲染「图标+文件名」chip（ATTACH_ICON 按原始文件类型），点击 openAttachment（async）：桌面端走 window.youdesignDesktop.openAttachment 桥（HTML 优先用 previewContent、注入 CSP+禁 <script>，ZIP 走 showItemInFolder reveal、其余走 openPath，落受控临时目录）；Web 端 createObjectURL + <a>。原始 Blob 存 UploadedImage/UploadedDoc.originalBlob，随 ChatMessage.attachments 持久化到 IndexedDB；后端不读 originalBlob。SESSION_LIMIT=50 封顶。
- **ZIP 原样打开不是执行打包应用**：home-helpers.ts 会从 ZIP 选 HTML，内联相对 CSS、图片、字体、媒体等资源；同时禁用脚本和 script preload/modulepreload，避免打包应用在 srcDoc/离线环境里执行后清空 DOM 造成白屏。因此它优先保留静态可见页面，不能假设 React/Vue 运行时仍会交互。
- **打开 HTML/ZIP 时不要把说明文字当修改**：rawHtml 首次打开只有检测到明确编辑词才调用 editHtmlSystemPrompt，否则只打开并问"想怎么修改"。intentSystemPrompt 进一步：**疑问句式**（"怎么/如何/可以…吗/能不能/是否/为什么/哪/怎样"）即使含"修改/改/调整"也判 ask。
- **Raw HTML 双表示已是一等状态**：GenerationResult.code/preview.html 是 iframe、导出和历史展示用的完整 HTML；GenerationResult.rawHtmlState.editHtml 是模型、Claude Code CLI 和点选修改用的资源占位 HTML，rawHtmlState.assets 负责回填 __YD_ASSET_<hash>__。改 raw HTML 编辑链路时优先用 getRawHtmlEditContext；直接编辑预览会清空 rawHtmlState，下一次 AI 编辑再重建。
- **上传 HTML/ZIP 固定不带组件库、风格档案由意图分类器决定是否套用**：HTML 上传时强制不引入组件库；**风格档案不被上传抹掉**——前端 HTML 上传只锁灰风格不锁，styleProfileId 一路透传到后端，由 classifyHtmlUploadIntent 的 applySelectedStyle 决定：regenerate 走正常生成管线自然套用已选风格；edit 且 applySelectedStyle=true 时把已选风格套到本次原页编辑；edit 且 applySelectedStyle=false / open / ask 仍按原页最小修改、不带风格。
- **原样 HTML 编辑必须失败不覆盖**：所有 HTML 编辑结果都要过 validateEditedHtmlDoc(original, edited, {deleteMode})，缺 doctype/html/body/</html>、标题/root 丢失、非删除场景输出 <原文 80%（体量骤降疑似截断/重写）都保留原页面，不生成新版本。纯删除指令（isDeleteInstruction）豁免 0.8 体量比。looksRewritten 同语义、同样接 {deleteMode}。
- **原样 HTML 点选修改用临时 anchor 精确定位**（内部仍称标注/annotation）：PreviewPane 点选元素时临时写 data-yd-anchor，把带 anchor 的 iframe HTML 作为 previous code 传给后端；htmlScopePatch.ts 优先用 anchor 找元素，再按"上方/下方/右边/同列/新增"等语义扩大到合理父级 scope；最终输出前必须 removeTemporaryAnchors。
- **点选修改选区只取「顶层可见元素及其祖先」**：PreviewPane.chooseMarkedElement 用 elementsFromPoint 取栈后，**必须用 el.contains(top) 收窄到顶层元素及其祖先链**，绝不允许 fall-through 到栈里被抽屉/弹窗/遮罩盖住的背景元素。
- **原样 HTML 大文件不整页编辑**：MAX_FULLPAGE_EDIT_BYTES=DEFAULT_FULLPAGE_EDIT_THRESHOLD_BYTES=640000（服务端，f100bb7 由 160KB 升至 640KB）——闸口统一走 HtmlSizeInfo，用户提示看 originalBytes。普通对话框编辑一旦原始 HTML ≥640KB 就跳过 tryScopePatch，统一发 desktop-claude-required；点选修改保持局部 scope patch 优先，只有未命中时才按**资源占位后的 compactBytes**判断整页编辑或 Claude handoff。超阈时不回退 editHtmlSystemPrompt 整页编辑。**桌面端 claude-html-utils.cjs 的 FULLPAGE_EDIT_THRESHOLD_BYTES 与 main.cjs 仍硬编码 160000、未同步升至 640KB**（见 §6）。
- **删除走快路径不调 LLM**：htmlScopePatch.ts 的 directDeleteElement 定位目标元素直接剪除（找"含多个同类兄弟的最近祖先"删一个，都没有则删锚点自身；重复兄弟按 tag+首 class token(基类)判定），确定性、任意大小可处理——LLM 重写大 scope 易截断。删除/移动指令还走 validateScopeReplacement({deleteMode:true})：下限放宽到 10%，no-op 检测用 scopeReplacementUnchanged。删除快路径校验失败才落到 LLM scope patch 兜底。
- **隐藏/确定性替换也走快路径不调 LLM**：tryScopePatch 在 LLM scope patch 之前先试两道确定性快路径，命中即 return：① directHideElement——"隐藏/不显示/不可见"指令在锚点元素注入 display:none !important；② tryDeterministicScopePatch——color/attr/text 三类在 scope 内**恰好命中 1 处**才替换。
- **对话式（无点选标注）编辑先试整页确定性快路径**：runEditRawHtml 对没有 anchor 标注的对话式编辑，进 scope patch/Claude 前先跑一次**整页** tryDeterministicScopePatch（裸「把 X 改成 Y」整页恰好命中 1 处、单一颜色、单一属性），0 LLM、与页面大小无关。
- **点选去重走 selectDedupScope、静态无重复升级整页**：EditPlan.operation=dedup 时 tryScopePatch 不走单点 scope patch，改走 selectDedupScope：沿锚点祖先链自内向外找"含≥2 个同签名直接子节点"的最小容器，让 LLM scope patch 在容器内一次性去重。**静态 HTML 无重复项**（返回 error）→ return null 升级整页。
- **全局视觉编辑意图改 LLM 判定、confidence 闸代码层强制**：runEditRawHtml 整页回退分支用 classifyGlobalVisualEditInstruction（LLM）判定是否走 editHtmlGlobalStylePrompt，取代旧 isGlobalHtmlEditInstruction 正则。**global===true && confidence>=0.8 才判全局**；null/低置信一律 false、保守走局部 editHtmlScopeSystemPrompt。
- **首次上传 HTML 的 edit 意图走 runEditRawHtml 且失败回吐原页**：classifyHtmlUploadIntent 判 edit 时走 runEditRawHtml（entry="first-upload"+openOriginalOnFailure=true）。首次上传的 desktop-claude-required 事件 stage 用 "open"（非 edit）。openOriginalAfterFailure 在任一失败分支回吐 code+preview+done（原页）。改首次上传编辑链路要保留这个回吐兜底。
- **删除被脚本按 id 引用的元素要守卫**：删 #drawerClose 等被 getElementById('drawerClose').addEventListener 引用的元素后，重渲染该行命中 null 抛 TypeError、中断后续初始化。htmlScopePatch.ts 的 guardDeletedIdScriptRefs(original, result) 在 runEditRawHtml 两个产出点对结果兜底：找出"原文有、结果无"的 id，若仍被 <script> 里 getElementById/querySelector('#id') 引用，就在第一个 <script> 前注入 null-safe 代理。
- **表格单元格/行锚点的 scope 不再无条件上扩到整表**：selectHtmlPatchScope 的 isTableEdit 只有指令含表格结构操作词（列/行/表头/单元格/同列/每行/位置词…）或 plan.scopeHint 命中 table 才上提到整张 <table>；否则按"目标元素"自身 scope（单元格改文案只改单元格）。加列/删列/合并/位置插入等结构操作仍走整表 scope。
- **删语义容器要关联删 overlay/mask 兄弟**：directDeleteElement 删 drawer/modal/dialog 等容器时，removeAssociatedOverlay 按 class 前缀找并删其遮罩兄弟。
- **JS 动态注入的内容要走持久编辑路径**：抽屉汇总卡片/表头/表格行/标题由 openDrawer() 从 deptData 运行时 innerHTML/textContent 注入；只改渲染 DOM 快照会在重渲染后被还原。detectScriptInjectedAnchor 扫锚点及祖先 id 是否被脚本赋值。命中后 runEditRawHtml 跳过局部 DOM 快路径，用 scriptInjectedAnchorPersistentInstruction 补强"优先修改脚本数据源、模板或渲染函数"，再走整页编辑或 desktop-claude-required。
- **删除整列走确定性跨行删，不靠 LLM**：「删除...这一列」+ td/th 锚点时，directDeleteColumn：按锚点单元格在其 tr 的列号，从表格每一行（含 thead）移除该列单元格，确定性、任意大小可处理；失败再落 LLM 表格 scope patch。列删除判定由 LLM editPlan.scopeHint 含 column 驱动（/列|栏/ 正则兜底，**不含「项」**——"删除这一项"通常指删一条记录行）。
- **点选修改对话框的「风格」选项是浅路（prompt 注入，非组件库生成）**：markup-pop 内复用首页 StyleProfilePicker；选中风格后 sendElement 把 styleProfileId 透传，runEditRawHtml 用 getStyleProfile 取 profile 传给 editHtmlScopeSystemPrompt/editHtmlSystemPrompt，buildStyleTokensSection 追加「按 X 风格重绘、只取设计 tokens、不套顶栏/侧栏、色值 inline 到元素 style」段；从 themeCss 提取品牌主色单独强调「必须用在强调元素」。**只对 modify/scope-patch + 全页编辑路径生效；删除快路径不调 LLM，风格无效**（UI 在删除词时灰掉 picker）。默认不选风格时 prompt 不变（additive）。
- **批量指令不走单点 scope patch**：locateScopeTarget 返回 loc.batch=true（"所有/每个/全部/每行/同列/这些/各"等）时，tryScopePatch 不做单点定位。批量文本替换的代表元素**必须是文案含被替换文本的可见元素**，**禁止**用 title/meta/head 或不含该文本的元素充当代表。
- **假成功检测**：prompts.ts 的 isTrivialNoOp 拦「模型原样回吐」——**内容归一（去所有空白）后相等**即 no-op，不报成功、不发新版本。**任何可见字符变化都不算 no-op**。scope patch 路径用同套逻辑 scopeReplacementUnchanged。整页编辑闸口只对占位压缩后 <640KB 的小文件生效（服务端；桌面端阈值未对齐、见 §6）。**tryScopePatch/runEditRawHtml 调用点的 isTrivialNoOp 闸门当前临时屏蔽**（见 §6）；桌面 Claude 路径的 isTrivialNoOp 不受影响。
- **点选指令的交互意图判定只看用户原话、指代继承不调模型**：resolveInteractiveHtmlEditInstruction 必须先 extractScopePatchTarget 剥出用户原话再判定。判定顺序：① 指代指令（"刚才/上面/前面/之前/那个/按…方式/照此/同样"）且带 sessionContext 且**无显式交互强信号词**→确定性继承当前版本最近对话的交互校验，**不调模型**；② 非指代指令→无条件走 classifyInteractiveEditIntent LLM；③ LLM 超时/null 但指令含无歧义交互强信号词→保守返回 true；灰区 conf 0.5–0.75 记日志、≥0.75 uplift。scope-edit 分支另补 isTrivialNoOp 兜底。
- **编辑前结构化计划（planHtmlEdit 路由单点/整页/Claude）**：原样/原生 HTML 点选修改在 tryScopePatch 里先调 planHtmlEdit（judges.ts，flash/temperature 0/maxTokens=900/withTimeout 5s，输出 EditPlan）。needsFullPage || interactive || batch 时直接 return null 跳过单点 scope patch；operation=dedup 不 return null、改走 selectDedupScope；否则把 plan 的目标/作用域/targetText/replacementText/selector/offset 拼进 locate 指令。编辑产出后过 validateEditedHtmlDoc 结构完整性校验；**isTrivialNoOp 空改检测与 hasInteractionDelta 交互增量检测当前临时屏蔽**（见 §6）；hasTabSwitchImplementation 页签类可执行切换检测未停用、仍生效。deleteMode 由 editPlan.operation（delete/move/dedup）触发、plan 缺席回退原 inline 正则。buildDomSummary 用等长空格 mask style/script/注释/doctype 保 offset 不漂移，每行前缀 [offset N] 并提取 role/aria-label/title/name/data-* 业务属性；matchLocateToAnchor 优先按 offsetHint 在 offset±800 找开标签定界。
- **生成请求失败重试只覆盖流内 error / 网络异常**：page.tsx 的 send 对 /api/generate 用 for (attempt <= GENERATE_RETRY_COUNT=2) 重试，但 catch 里 isAbortError(err) || err instanceof HttpResponseError || attempt >= max 直接 throw——**HTTP 非 2xx 走 HttpResponseError 不重试**，只有流式过程中收到 case "error" 事件（PipelineEventError）或 fetch 网络层异常才等 GENERATE_RETRY_DELAY_MS=500 重试。每次 attempt 单独维护 terminalResponseReceived：收到 clarify/done 或 contextTurn:"accepted" 的 assistant 后，即使随后 reader/JSON 断流也直接结束本请求。用户取消不重试。
- **DPL/JSX runEdit 的 Phase-1 护栏（防闲聊覆盖/防空改/防整页重写）**（历史 DPL 路径铁律，原生 HTML 同语义适用 looksLikeHtml/validateEditedHtmlDoc）：① 模型返回非可解析 HTML（looksLikeHtml 不通过）-> 不覆盖原页、回澄清、不发 code/done；② isTrivialNoOp（归一相等）-> 保留原页、不产新版本；③ looksRewritten（长度比 <0.55，deleteMode 豁免合法大删除）判整页重写 -> 升级 generate 阶段强模型带「逐字保留基底、只插入本次要求」纠正指令重试一次。改 runEdit/looksRewritten 别退回「模型吐啥就覆盖啥」。
- **data URI 占位压缩（送模型边界）**：dataUriPlaceholder.ts 的 compactDataUris 把 base64/字体等 data: URI 换成稳定 __YD_ASSET_<hash>__ 占位符，送模型前压缩、输出后 expandDataUris 回填。MIN_SAVING=2048（省 >2KB 才启用）。**绝不修改 original**。editHtmlSystemPrompt/editHtmlScopeSystemPrompt 检测到占位符时追加规则要求模型原样保留。
- **禁止外部占位图服务**：prompts.ts 的 imagePlaceholderRule 铁律——**严禁** <img src="https://..."> 指向 via.placeholder.com（已停服）/ placehold.co / picsum / unsplash 等任何外链图。需要图片占位按优先级：① 内联 SVG 或 data URI；② 纯文字 + emoji/图标（📄 发票、🧾 单据、头像用姓名首字圆形）。改生成提示词不要去掉这条。
- **布局切换**：page.tsx 顶栏可切两栏/三栏（localStorage.yd_layout_mode），三栏需 canUseThreeColumn 才生效、否则降级两栏。
- **UI 主题 ≠ 产品风格档案（两套别混淆）**：src/lib/theme/（themes.ts 5 套）是 **YouDesign 应用外壳自身**的配色/字体/圆角，通过 CSS 变量写在 :root + data-theme，ThemeProvider 在 layout.tsx 全局注入、localStorage key yd_theme 持久化、默认 classic；用户在预览头右侧「更多」菜单切换。它**只影响外壳，不进生成管线、不注入 prompt**。而 src/lib/style/profiles.ts 的 8 个**产品风格档案**才是影响**生成原型**视觉的（注入 styleSpec/themeCss 到 generate prompt）。
- **Anthropic 流式**：providers/anthropic.ts 的 stream() 用流式输出，避免长连接被中间层切断。DeepSeek/GLM 走 openaiCompatible 的流式。改 provider 流式要保证 StreamDelta 契约一致。
- **HTML 自动定位阈值与打分**：LOCATE_MIN_HTML=25000（<25KB 不走 auto-locate），LOCATE_MIN_CONFIDENCE=0.6。buildDomSummary 上限 30KB。matchLocateToAnchor 收集所有候选按多信号打分（id 5 > class 4 > text 2 > tag 1）取最高。
- **上传上限与单文件打包器解包**：home-helpers.ts 的 MAX_UPLOAD_MB=5，page.tsx 的 MAX_ATTACHMENTS=4。单文件打包器（<script type="__bundler/template"> 藏整份 HTML 转义成 JS 字串 + <script type="__bundler/manifest"> 资源 gzip-base64 清单）必须先解包——否则 buildDomSummary 剥掉 script 只看到脚手架、自动定位找不到目标、整页闸口因体积超标直接放弃。
- **预览内直接编辑/点选修改只支持原生/原样 HTML**：直接编辑已升级为单元素属性面板（不再设置整页 body contenteditable）。临时选中属性与 <style> 使用每次会话随机 token，实际文档按节点引用恢复、序列化副本只剥离 token 精确匹配节点，禁止固定 id 或无值属性全局清理。
- **品牌色保真两道闸**：① prompts.ts 的 colorFidelityRule 强制逐字照抄规范色值；② validate.ts 的 normalizeBrandColors 在生成/review/edit 后按 themeCss 饱和 token 就近吸附（RGB 曼哈顿距离 ≤ COLOR_SNAP_THRESHOLD=48），中性灰（max-min ≤ 30）要求精确匹配。改 profiles.ts/themeCss 后这两个闸口自动跟进。
- **导出 HTML 必须离线可开**：原生 HTML 直接导出自包含文件。
- **临时产物统一放 output/**：自动化测试脚本与报告、截图、PDF 导出等一切「为本次任务临时产出、不进运行时」的文件，一律写入 output/ 下相应子目录，不要散落在仓库根或新建 tmp/ 之类目录。output/ 已 gitignore。
- **需求评审卡集随原型版本走三套继承语义**：requirementCardSet 挂在 GenerationResult 上、随版本回退一起回退。原型真更新 → inheritRequirementCardSet：卡集状态置 inherited、非作废卡 reviewStatus 重置 pending、所有 link 锚点 valid:false；artifact-only 兜底 → carryOverRequirementCardSet：只推进 artifactVersion，不重置状态、不失效锚点；直接改文案等轻量改动 → inherit(..., {preserveReviewStatus:true})。手工建卡编号 BR-xx 单调递增。
- **需求卡「关联区块」用语义锚点、跨 realm 判定要用 iframe 自己的构造器**：点选关联存 selector/domPath/text/role/rect 五元组；locateRequirementLink 依次试 selector→domPath→text（text 精确匹配优先）。iframe 元素属于 iframe 自己的 realm，父窗口 instanceof HTMLElement 恒 false——必须取 doc.defaultView.HTMLElement。导出/分享勾选「附带需求卡」时 buildRequirementCardsSectionHtml 把评审面板+marker+连线脚本注入 </body> 前：卡内容逐字段转义防 XSS、JSON 数据里 < 替换为 < 防 </script> 注入。
- **绝不提交**：`.env.local`（密钥/口令/MySQL 凭据）、`data/`（users.json、prices.json、stage-timing.jsonl 等环境本地数据）、`vendor/`、`output/`/`.playwright-cli/`（本地产物）、`.next`、`node_modules`、`dist-desktop*/`（桌面打包产物）。`.env.example` 是**占位符模板**（无真实凭据）**入库**。
- 改提示词 / 模型路由会直接影响**原型保真度**——改前后请人工对比几个典型需求（后续会接入 eval）。
- **多用户/计量层**：YOUDESIGN_AUTH_MODE=auto 时由 data/users.json 是否有启用用户决定（isMultiUserMode）；无则回退单口令 YOUDESIGN_ACCESS_PASSWORD。计量：/api/generate 用 runWithMeter 注入 AsyncLocalStorage，getProviderForStage 在有 userId 上下文且非 mock 时返回 MeteredProvider 包装——15 个 LLM 调用点零改动、重试循环自动覆盖；provider 经 CompletionOptions.onUsage 回调吐 token 用量，MeteredProvider 流末尾等待一次 MySQL usage_records 写入（或回退 JSONL），写失败只日志不阻断生成，computeCost 按 data/prices.json 算 CNY 费用。数据库时间统一存 UTC，看板 SQL 用 DATE_ADD(..., INTERVAL 8 HOUR) 按中国自然日聚合（兼容现网 MySQL 5.6）。**embedding 未计量**，待补。改认证/计量要同步 src/lib/auth/*、src/lib/meter/*、scripts/user.mjs、本文件。data/ 仅继续承载 users.json、prices.json 与 stage-timing.jsonl。

## 5. 协作约定（多人 + 多 AI 工具）

- **一功能一 PR**，base `main`；PR 前必须 `typecheck` + `build` 通过。
- **分支命名**：`feat/...`、`fix/...`、`docs/...`、`chore/...`。
- **PR 描述**：说清「改了什么 / 为什么 / reviewer 注意」；若动了 `prompts.ts` 或 `ROUTE_*`，在描述里点明并说明已人工/eval 验证。
- **提交署名**：各 AI 工具**保留自己的 `Co-Authored-By` 行**；人类提交可不带。
- **不要在 `main` 直接改**；不要把"格式化整文件"和"功能改动"混在一个 PR（避免巨大 diff）。
- **大文件**：`orchestrator.ts` / `page.tsx` / `desktop/main.cjs` 已拆出 intent.ts/routing.ts/timing.ts、page-utils.ts/desktop-claude.ts/PrototypeControls.tsx、desktop-*-utils.cjs；`globals.css` 仍偏大。继续尽量只改相关片段。

## 6. 当前已知待办（避免重复造轮子）

- **提速**：快速/高质量开关已上线；阶段耗时埋点已上线（data/stage-timing.jsonl）；下一步可做多页按页并行生成、精简注入的 styleSpec、按 stage-timing 实测调路由、建立 fast/high-quality 对照 eval，避免为了速度牺牲保真度。
- **reviewAndRefine 临时整体跳过中**：恢复时删 orchestrator.ts reviewAndRefine 函数头的 return code + 整段块注释符，恢复后跑 npm run test:prototype-contract 并人工对比保真度。
- **nav-repair 临时停用中**：repairUnsafeHtmlNavigation 在 runGenerate 与 runEditRawHtml 两条路径都被块注释停用。预览期跳转守卫（previewNavigation.ts）仍兜底。恢复：移除 orchestrator.ts 两处 /* ... */ 块注释符（搜【临时停用】nav-repair），恢复后跑 npm run test:preview-navigation。
- **no-op / 交互闸门临时屏蔽中**：tryScopePatch 与 runEditRawHtml 的 isTrivialNoOp（空改检测）+ hasInteractionDelta（交互增量检测）两道闸门被注释屏蔽——hasInteractionDelta 仅认 6 种事件模式，复用已有 handler / <details> / CSS / 非 listed 事件被误判无交互改动而丢弃模型已完成的修改。hasTabSwitchImplementation 与桌面 Claude 路径的 no-op 双出口不受影响。恢复：移除 orchestrator.ts 两处 // 注释（搜【临时屏蔽】no-op 闸门），**拓宽 hasInteractionDelta 事件模式后再开**；恢复后跑 npm run test:desktop-claude-core + test:prototype-contract。
- **structure-check 临时无条件跳过中**（SKIP_NON_CLAUDE_STRUCTURE_CHECK=true）：structureCheckStage 函数头直接 return []、checkGeneratedStructure 不再调用。恢复：删 orchestrator.ts 的 SKIP_NON_CLAUDE_STRUCTURE_CHECK 常量或置 false，恢复后跑 npm run test:prototype-contract。
- **桌面端整页阈值未同步升至 640KB**：f100bb7 把服务端 DEFAULT_FULLPAGE_EDIT_THRESHOLD_BYTES 由 160KB 升到 640KB，但 desktop/claude-html-utils.cjs 的 FULLPAGE_EDIT_THRESHOLD_BYTES 与 desktop/main.cjs 仍硬编码 160000，服务端与桌面端对整页编辑/Claude handoff 的体量判定不一致。对齐：把桌面两处常量也改为 640000。
- **历史 DPL 校验死代码待清理**：validate.ts/structureCheck.ts 仍含受 useDpl=false 守卫的 DPL 组件名校验分支（invalidImports/invalidFormApis/adminSkeletonIssues 等 useDpl=true 分支），自 api/generate 不再传 useDpl=true 起不可达、无害。清理需跨 validate.ts/structureCheck.ts/orchestrator.ts 删 useDpl 参数与 DPL 分支，并同步删 test:prototype-contract 的相关锁定断言，作为单独 PR 做。
- **需求评审卡**：目前纯手工建卡/关联；AI 从需求与 Prototype Contract 自动提炼初版评审卡、卡片与 mustHave 联动验收尚未做。
- 代码规范统一（Prettier + ESLint）、CI（typecheck/build/lint/密钥扫描）、main 分支保护——**尚未配**。
- 生成质量 **eval**（固定需求 + 断言）——尚无；改 prompt/路由的回归全靠人工。
- 单元测试——尚无；可先覆盖 validate（含 device 防幻觉）/ extractHtmlDoc / isComplexStructure 等确定性逻辑。
- 会话持久化仍**纯前端、按浏览器**：换设备/清缓存看不到历史。身份与用量统计已上服务端，会话内容跨设备同步尚未做（按需）。
- **抓取扩展 v0.2.9 已知局限**：引导式多页签采集只识别标准 role=tablist / Ant Design 页签（2-16 个 tab），自定义页签实现不识别；drawer_tracker.js 经 content_scripts 在所有 http(s) 页 document_start 注入（全域加载期跟踪点击，抽屉 observed-click 映射对所有页面生效）；抓取页离线交互是浅层 runtime（抽屉开关/页签切换/键盘导航），不还原表单提交/数据请求/动态加载；引导式采集的默认已打开页签须等页面稳定加载完成才能开始；**污染 canvas 截图兜底只对抓取瞬间可见的图保真**；扩展改完需在 chrome://extensions Reload。
- **桌面正式发布待做**：Apple Developer ID 签名、公证、正式应用图标、自动更新通道、崩溃/日志策略、隐私/数据说明、安装包验收清单。未完成前 desktop:pack:mac / desktop:dmg:mac 只作为本地验证包。
- **embedding 计量未做**：当前计量仅覆盖 LLM chat 调用（15 处）；智谱 embedding-3 未计入费用，占比极小且仅 ZHIPU_API_KEY 配置时触发，待按需给 src/lib/embeddings/ 加 onUsage 钩子。
- 沙箱可继续：编译/运行时错误回流自修复、prop 级校验、更多组件实测覆盖。
- 拆大文件（orchestrator.ts/page.tsx/main.cjs 已抽模块，globals.css 待拆）、Node 版本锁（.nvmrc/engines）、CONTRIBUTING.md / PR 模板。

> 维护本文件：当你发现一个新「坑」或定下一个新约定，请顺手更新这里——这是团队 + 所有 AI 的共享记忆。
